export const emailRegexp = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i;
export const emailMessage = 'This is an ERROR email';
export const requiredMessage = 'This field is required';
export const passwordMinLength = 'Password must be at least 6 characters';
export const passwordMaxLength = 'Password must not exceed 16 characters';
export const passwordUppercase = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)/;
export const passwordUppercaseMessage =
  'Password must contain at least 1 uppercase letter';
